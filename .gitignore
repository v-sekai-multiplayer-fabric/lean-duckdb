/.lake/
/build/
*.olean
# The whole vendor/ tree is a build artifact: scripts/build-duckdb-static.sh
# fetches the DuckDB amalgamation + extension SOURCE and compiles the encapsulated
# libduckdb.a there on a plain `lake build`. Nothing under vendor/ is committed.
/vendor/
